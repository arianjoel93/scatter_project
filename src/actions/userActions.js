import { REQUEST_ADDED, REQUEST_ADDED_SUCCESSFULLY, REQUEST_ADDED_ERROR } from "../types/index"

//Create new request

export function createNewRequestAction(){
    return ()=>{
        console.log('desde action')
    }
}